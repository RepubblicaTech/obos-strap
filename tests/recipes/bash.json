{
    "name": "bash",
    "description": "Bourne Again SHell",
    "version": [ 5,3,0 ],
    "url": "https://mirror.csclub.uwaterloo.ca/gnu/bash/bash-5.3.tar.gz",
    "depends": [],
    "build-depends": [],
    "patches": [],
    "bootstrap-commands": [
        [ "${repo_directory}/bash-5.3/configure", "--prefix", "${prefix}" ]
    ],
    "build-commands": [
        [ "make", "all" ]
    ],
    "install-commands": [
        [ "make", "install", "DESTDIR=${destdir}" ],
        [ "make", "install", "DESTDIR=${bin_package_prefix}" ]
    ]
}
